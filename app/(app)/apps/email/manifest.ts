import { AppManifest } from "../common"

export const manifest: AppManifest = {
  name: "Email Monitoring",
  description: "Connect to email servers and monitor incoming emails with file attachments",
  icon: "📧",
}
